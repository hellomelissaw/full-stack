doctype html
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    link(rel="stylesheet" href="static/startpage.css")
    title HL3
    //- The second title tag is redundant and has been removed.
  body
    .screen
      .menu
        h1 Best game ever
        h3 A group 16 creation
        ul
          li
            a(href=`/location?locID=${userLocID}`) New Game
          li
            a(href=`/location?locID=${userLocID}`) Load Game
          li
            a(href="#" id="quit-btn") Quit
    script(src="logic.js")